// -----------------------------------------------------------------------------
// tests.ts
// -----------------------------------------------------------------------------
import * as test from 'tape';

test('test',(t)=>{
    t.true( true, "test true 실패" );
    t.equal( 1,1,"test equal 실패" );
    t.end();
});
